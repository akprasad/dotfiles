[alias]
    a = add
    au = add -u
    b = branch
    ci = commit
    co = checkout
    d = diff
    dc = diff --cached
    s = status --short
    st = stash
    tree = log --graph --oneline --all --decorate
    pop = stash pop
    last = log -n 1

